# /implement — Execute a paiOS M0 Task

Load skill **paios-implement** (`.claude/skills/paios-implement/SKILL.md`; SSoT: `.agents/skills/paios-implement/SKILL.md`).

Execute the task whose ID is given as the argument (e.g. `/implement 3`). If no ID is given, run `task-master next --tag m0` to find the next unblocked pending task and confirm before starting.
