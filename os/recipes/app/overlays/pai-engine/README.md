# pai-engine app overlay (unused)

This overlay directory is no longer used by the build. The pai-engine binary and systemd unit are now installed via a Debian package.

The packaging source lives at `os/packaging/pai-engine/`. Run `os/packaging/build-deb.sh` to produce the `.deb` before running debos.
